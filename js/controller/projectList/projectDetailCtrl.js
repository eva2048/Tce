define(['app'], function(app) {
            app.register
                .controller('projectDetailCtrl',
                    function($scope) {
                        $scope.isAttribute = true;
                    })
                })
